import React from "react";

const ExpenseList = () => {
    return (
        <div>
            <h2>Expense List</h2>
            {/* Expense items will be listed here */}
        </div>
    );
};

export default ExpenseList;  // Ensure there's an export
